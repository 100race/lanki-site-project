package com.race.site;

import org.springframework.web.bind.annotation.RestController;


@RestController
public class SiteController {

	/*
	 * @GetMapping("/create") public ResponseEntity<SiteVO>
	 * create(@PathVariable("sitegrpno"))
	 * 
	 * @PostMapping("/{sitegrpno}") public ResponseEntity<String>
	 * create(@RequestBody SiteVO vo){
	 * 
	 * Log.info("SiteVO1:"+ vo.getLink()); Log.info("SiteVO1:"+ vo.getSname());
	 * Log.info("SiteVO1:"+ vo.getContent());
	 * 
	 * vo.setContent(vo.getContent().replaceAll("/n/r","<br>"));
	 * 
	 * int flag = service.create(vo); }
	 */
}
